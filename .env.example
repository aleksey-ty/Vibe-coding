# Пример файла окружения для backend.
# Скопируйте его в .env и подставьте свой ключ OpenRouter.
# Реальный .env в Git не попадает (см. .gitignore).
OPENROUTER_API_KEY=
OPENROUTER_MODEL=deepseek/deepseek-v4-flash-0731

# Необязательно: порт backend. По умолчанию 3001.
# PORT=3001
